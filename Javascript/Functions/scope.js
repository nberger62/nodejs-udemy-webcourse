function doMath(){
    var x = 40;
    console.log(x);
}
doMath()

/*
40
*/

var x = "hello!";

/*
the variable x changes to hello, but when you run the function x is 40. That is because of the scope within the function
*/
